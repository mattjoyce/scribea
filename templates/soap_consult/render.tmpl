{# Jinja2 template — see templates/soap_consult/template.json for the contract. #}
# SOAP note — {{ session_id }}

**Template:** soap_consult v{{ template_version }}
**Recorded:** {{ started_at }}
**Duration:** {{ duration_human }}
**Clips:** {{ clip_count }}{% if gaps %} ({{ gaps }} with missing audio){% endif %}
**Model:** {{ model }}{% if prompt_version %} (prompt {{ prompt_version }}){% endif %}

---

## Subjective

{{ subjective }}

## Objective

{{ objective }}

## Assessment

{{ assessment }}

## Plan

{% for step in plan %}{{ loop.index }}. {{ step }}
{% endfor %}

---

*Generated by scribea — not for clinical use.*
